import * as React from "react";

export default class Users extends React.Component<{children?: React.ReactNode}> {
	render() {
		return (
			<div className="users-zCtORhtM8B4Y3lYB">
				{this.props.children}
			</div>
		);
	}
}