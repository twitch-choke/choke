import * as React from "react";

export default class Tabs extends React.Component<{children: React.ReactNode}> {
	render() {
		return (
			<span className="tabs-152TJMp0bSun8qbx">
				{this.props.children}
			</span>
		);
	}
}